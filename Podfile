# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CoordinatorTestApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "Resolver"
  # Pods for CoordinatorTestApp

  target 'CoordinatorTestAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CoordinatorTestAppUITests' do
    # Pods for testing
  end

end
